import SEOContentAnalyzer from "./SEOContentAnalyzer";

function ImageAltTextGenerator() {
  return <SEOContentAnalyzer initialTab="audit" />;
}

export default ImageAltTextGenerator;
// (Alt texts can be generated or checked, in this case it runs the HTML element check)
